<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.co.ppu.collectionfast2">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- For keeping the LocationTracker alive while it is doing networking -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".login.WelcomeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".lkp.ActivityScrollingLKPDetails"
            android:label="@string/title_activity_activity_scrolling_lkpdetails"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".payment.ActivityPaymentEntry" />
        <activity android:name=".lkp.ActivityVisitResult" />
        <activity android:name=".lkp.ActivityChangeAddress" />
        <activity android:name=".lkp.ActivityRepoEntry" />
        <activity android:name=".lkp.ActivityPaymentHistory" />

        <receiver android:name=".job.SyncJob" />
        <receiver android:name=".location.LocationTracker" />

        <activity android:name=".lkp.ActivityVehicleInfo" />
        <activity android:name=".lkp.ActivityUploadPicture" />
        <activity android:name=".lkp.ActivityVisitResultRPC"></activity>
    </application>

</manifest>